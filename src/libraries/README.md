# Libraries

All the libraries here are hand written (not coppied from other devs)  
The reason we have these are to remove the need of external dependcies for the language and its apps.
Any runtime helpers go in `runtime_helpers.rs`  