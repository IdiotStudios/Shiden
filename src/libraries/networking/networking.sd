# HTTP Client Operations
fn new net http get(url)/
    __net_http_get(url)/
fn/

fn new net http post(url, body)/
    __net_http_post(url, body)/
fn/

fn new net http put(url, body)/
    __net_http_put(url, body)/
fn/

fn new net http delete(url)/
    __net_http_delete(url)/
fn/

# HTTP Server Operations
fn new net server new(port)/
    __net_server_new(port)/
fn/

fn new net server route(server, path, method)/
    __net_server_route(server, path, method)/
fn/

fn new net server start(server)/
    __net_server_start(server)/
fn/

fn new net server stop(server)/
    __net_server_stop(server)/
fn/

# File Serving
fn new net file serve_dir(path, port)/
    __net_file_serve_dir(path, port)/
fn/

# JSON Operations
fn new net json parse(data)/
    __net_json_parse(data)/
fn/

fn new net json stringify(obj)/
    __net_json_stringify(obj)/
fn/

fn new net json get(obj, key)/
    __net_json_get(obj, key)/
fn/

fn new net json set(obj, key, value)/
    __net_json_set(obj, key, value)/
fn/

# URL Operations
fn new net url parse(url)/
    __net_url_parse(url)/
fn/

fn new net url encode(str)/
    __net_url_encode(str)/
fn/

fn new net url decode(str)/
    __net_url_decode(str)/
fn/

# WebSocket Operations
fn new net websocket connect(url)/
    __net_websocket_connect(url)/
fn/

fn new net websocket send(ws, data)/
    __net_websocket_send(ws, data)/
fn/

fn new net websocket close(ws)/
    __net_websocket_close(ws)/
fn/

# TCP Socket Operations
fn new net tcp server new(port)/
    __net_tcp_server_new(port)/
fn/

fn new net tcp server accept(server)/
    __net_tcp_server_accept(server)/
fn/

fn new net tcp connect(host, port)/
    __net_tcp_connect(host, port)/
fn/

fn new net tcp send(socket, data)/
    __net_tcp_send(socket, data)/
fn/

fn new net tcp recv(socket)/
    __net_tcp_recv(socket)/
fn/

fn new net tcp close(socket)/
    __net_tcp_close(socket)/
fn/

# UDP Socket Operations
fn new net udp socket new(port)/
    __net_udp_socket_new(port)/
fn/

fn new net udp send(socket, addr, data)/
    __net_udp_send(socket, addr, data)/
fn/

fn new net udp recv(socket)/
    __net_udp_recv(socket)/
fn/

fn new net udp close(socket)/
    __net_udp_close(socket)/
fn/

# Web Scraping Operations
fn new net scrape page(url)/
    __net_scrape_page(url)/
fn/

fn new net scrape extract_text(html)/
    __net_scrape_extract_text(html)/
fn/

fn new net scrape extract_links(html)/
    __net_scrape_extract_links(html)/
fn/

fn new net scrape select(html, selector)/
    __net_scrape_select(html, selector)/
fn/

fn new net scrape attr(html, selector, attr)/
    __net_scrape_attr(html, selector, attr)/
fn/