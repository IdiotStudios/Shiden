# shiden.toml - project manifest
[project]
name = "http-server"
version = "0.1.0"
type = "binary"

[build]
opt_level = 3
targets = ["x86_64-linux", "x86_64-windows", "x86_64-macos"]

[dependencies]
# "pkg" = "version"
