fn new run(code)/
    let mut cmds = []/array
    for c in code/
        push(cmds, c)/
    for/

    let mut tape = []/array
    push(tape, 0)/
    let mut ptr = 0/i64
    let mut ip = 0/i64

    let mut n = len(cmds)/i64
    while ip < n/
        let cmd = cmds[ip]/char

        if cmd == '>'/
            ptr = ptr + 1/
            while ptr >= len(tape)/
                push(tape, 0)/
            while/
        if/

        if cmd == '<'/
            if ptr == 0/
                ip = n/
            if/
            ptr = ptr - 1/
        if/

        if cmd == '+'/
            let v = tape[ptr]/i64
            tape[ptr] = v + 1/
        if/

        if cmd == '-'/
            let v = tape[ptr]/i64
            tape[ptr] = v - 1/
        if/

        if cmd == '.'/
            println("{}", tape[ptr])/
        if/

        if cmd == ','/
        if/

        if cmd == '['/
            if tape[ptr] == 0/
                let mut depth = 1/i64
                let mut j = ip + 1/i64
                while j < n/
                    let c2 = cmds[j]/char
                    if c2 == '['/
                        depth = depth + 1/
                    if/
                    if c2 == ']'/
                        depth = depth - 1/
                        if depth == 0/
                            ip = j/
                            break/
                        if/
                    if/
                    j = j + 1/
                while/
            if/
        if/

        if cmd == ']'/
            if !(tape[ptr] == 0)/
                let mut depth = 1/i64
                let mut j = ip - 1/i64
                while j >= 0/
                    let c2 = cmds[j]/char
                    if c2 == ']'/
                        depth = depth + 1/
                    if/
                    if c2 == '['/
                        depth = depth - 1/
                        if depth == 0/
                            ip = j/
                            break/
                        if/
                    if/
                    j = j - 1/
                while/
            if/
        if/

        ip = ip + 1/
    while/
fn/